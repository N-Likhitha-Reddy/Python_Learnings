
import random

list = []
for i in range(1000):
    x = random.randrange(1,6)
    list.append(x)

#PENDING - Use the collection module to count the frequency of each possible sum(2 through 12) and print the results.
